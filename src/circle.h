#ifndef __CIRCLE_H
#define __CIRCLE_H

#include <math.h>

#include "fbuffer.h"
#include "pixel.h"

void circle(int, int, int, uint32_t);
void circle_fill(int, int, int, uint32_t);

#endif // __CIRCLE_H
